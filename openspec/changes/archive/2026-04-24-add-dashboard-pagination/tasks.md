## 1. 实现分页状态

- [x] 1.1 在新标签页组件中新增 `PageSize` 类型、默认每页 20、当前页状态。
- [x] 1.2 将每页数量限制为 20、50、100。
- [x] 1.3 每页数量变化时重置到第一页。

## 2. 接入课程切片

- [x] 2.1 在排序后的课程视图上计算总页数、有效页码和当前页数据。
- [x] 2.2 将课程渲染从全量 `courseViews` 改为当前页数据。
- [x] 2.3 当同步或本地变化导致页码越界时自动回落。

## 3. 增加分页 UI

- [x] 3.1 增加分页范围文案，例如 `1-20 / 86`。
- [x] 3.2 增加上一页和下一页按钮。
- [x] 3.3 增加每页数量选择控件：20、50、100。
- [x] 3.4 保持网格和列表布局对齐。

## 4. 验证

- [x] 4.1 运行 `openspec validate add-dashboard-pagination --strict`。
- [x] 4.2 运行 `npm run build`。
- [x] 4.3 手动检查空状态、少于 20 条、超过 20 条、切换 50/100、最后一页不足一页。
