## 1. 数据模型

- [x] 1.1 为 `LearningCourseRecord` 增加 `createdAt`。
- [x] 1.2 新建学习记录时写入 `createdAt`，更新已有记录时保留旧值。
- [x] 1.3 旧记录缺少 `createdAt` 时用 `updatedAt` 回退。

## 2. 排序状态

- [x] 2.1 新增排序字段类型：更新时间、添加时间、学习进度、学习时长。
- [x] 2.2 新增排序方向类型：升序、降序。
- [x] 2.3 将排序偏好保存到 `localStorage`。

## 3. 排序与分页协同

- [x] 3.1 在分页切片前执行排序。
- [x] 3.2 排序配置变化时重置到第一页。
- [x] 3.3 字段值相同时使用更新时间降序作为稳定回退。

## 4. UI

- [x] 4.1 在分页栏增加排序字段选择控件。
- [x] 4.2 在分页栏增加升序/降序切换控件。
- [x] 4.3 保持分页栏和列表底部对齐。

## 5. 验证

- [x] 5.1 运行 `openspec validate add-dashboard-sorting --strict`。
- [x] 5.2 运行 `npm run build`。
- [x] 5.3 手动检查四种排序字段、升降序、分页重置和旧记录兼容。
